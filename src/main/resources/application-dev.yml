spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authen_app
    username: root
    password: ${DB_PASS}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

cookie:
  creation:
    secure: false
    same-site: Lax
